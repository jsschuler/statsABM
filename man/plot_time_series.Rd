% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_plotting.R
\name{plot_time_series}
\alias{plot_time_series}
\title{Plot time series from a model results tibble}
\usage{
plot_time_series(
  results,
  title,
  colour_map = NULL,
  log_y = FALSE,
  theme_fn = theme_course_light
)
}
\arguments{
\item{results}{A long-format tibble with columns: \code{time} (or \code{step}),
\code{variable}, \code{value}.}

\item{title}{Plot title string.}

\item{colour_map}{Named character vector mapping variable names to hex
colours. Defaults to \code{COURSE_PALETTE}.}

\item{log_y}{Logical; use log scale on y-axis. Default \code{FALSE}.}

\item{theme_fn}{A ggplot2 theme function to apply. Default
\code{theme_course_light}.}
}
\value{
A ggplot object.
}
\description{
Plot time series from a model results tibble
}
\examples{
results <- tibble::tibble(
  time     = rep(1:10, 2),
  variable = rep(c("prey", "predators"), each = 10),
  value    = c(runif(10, 30, 50), runif(10, 5, 15))
)
plot_time_series(results, title = "Example")
}
