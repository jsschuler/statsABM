% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sd_isr_pandemic.R
\name{run_isr}
\alias{run_isr}
\title{Run the ISR compartmental ODE model}
\usage{
run_isr(
  params = ISR_DEFAULTS,
  y0 = c(I = 0.01, S = 0.98, R = 0.01),
  t_span = c(0, 160),
  n_points = 1600L
)
}
\arguments{
\item{params}{Named list. Defaults to \code{ISR_DEFAULTS}.
Elements: \code{beta} (transmission rate), \code{gamma} (recovery rate).}

\item{y0}{Named numeric vector of initial fractions.
Default \code{c(I = 0.01, S = 0.98, R = 0.01)}. Must sum to 1.}

\item{t_span}{Numeric vector \code{c(start, end)}. Default \code{c(0, 160)}.}

\item{n_points}{Integer number of output time points. Default \code{1600}.}
}
\value{
Named list:
\itemize{
\item \code{results}: long-format tibble with columns \code{time}, \code{variable}, \code{value}
(\code{variable} in \code{c("I", "S", "R")})
\item \code{R0}: basic reproduction number (\code{beta / gamma})
\item \code{peak_I}: peak infected fraction
\item \code{peak_time}: time of peak infected fraction
\item \code{final_R}: fraction recovered at end of simulation
\item \code{params}: the params list used
}
}
\description{
Integrates the ISR (Infected-Susceptible-Recovered) epidemic ODE using
\code{deSolve::ode()}. Compartment ordering is ISR, not SIR.
}
\examples{
out <- run_isr()
out$R0
out$peak_I
}
