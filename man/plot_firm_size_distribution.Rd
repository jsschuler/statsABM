% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_axtell.R
\name{plot_firm_size_distribution}
\alias{plot_firm_size_distribution}
\title{Plot a firm-size distribution on log-log axes with power-law fit}
\usage{
plot_firm_size_distribution(
  firm_sizes,
  fit = NULL,
  title = "Firm size distribution",
  theme_fn = theme_course_light
)
}
\arguments{
\item{firm_sizes}{Integer vector of firm sizes (one element per firm).}

\item{fit}{Optional list from \code{fit_power_law()}; if supplied, overlays
the fitted line. Must contain elements \code{alpha} and \code{xmin}.}

\item{title}{Plot title. Default \code{"Firm size distribution"}.}

\item{theme_fn}{A ggplot2 theme function to apply. Default
\code{theme_course_light}.}
}
\value{
A ggplot object.
}
\description{
Displays the complementary CDF (survival function) of firm sizes on
log-log axes. If a power-law fit is supplied (from \code{fit_power_law()}),
overlays the fitted line.
}
\examples{
sizes <- round(poweRlaw::rplcon(500, xmin = 1, alpha = 2.1))
plot_firm_size_distribution(sizes)
}
