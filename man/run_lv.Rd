% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sd_lotka_volterra.R
\name{run_lv}
\alias{run_lv}
\title{Run the Lotka-Volterra ODE system}
\usage{
run_lv(
  params = LV_DEFAULTS,
  y0 = c(prey = 40, predators = 9),
  t_span = c(0, 200),
  n_points = 2000L
)
}
\arguments{
\item{params}{Named list of parameters. Defaults to \code{LV_DEFAULTS}.
Elements: \code{alpha} (prey birth rate), \code{beta} (predation rate),
\code{gamma} (predator death rate), \code{delta} (predator birth per prey eaten).}

\item{y0}{Named numeric vector of initial conditions.
Default \code{c(prey = 40, predators = 9)}.}

\item{t_span}{Numeric vector \code{c(start, end)}. Default \code{c(0, 200)}.}

\item{n_points}{Number of output time points. Default \code{2000}.}
}
\value{
Named list:
\itemize{
\item \code{results}: long-format tibble with columns \code{time}, \code{variable}, \code{value}
(\code{variable} is \code{"prey"} or \code{"predators"})
\item \code{equilibrium}: named vector \code{c(prey = ..., predators = ...)}
\item \code{params}: the params list used
}
}
\description{
Integrates the classical Lotka-Volterra predator-prey equations using
\code{deSolve::ode()} with the \code{lsoda} method.
}
\examples{
out <- run_lv()
head(out$results)
out$equilibrium
}
