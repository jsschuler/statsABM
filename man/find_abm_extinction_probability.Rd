% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_activities.R
\name{find_abm_extinction_probability}
\alias{find_abm_extinction_probability}
\title{Estimate ABM extinction probability across replicate runs}
\usage{
find_abm_extinction_probability(
  params,
  n_runs = 20L,
  steps = 500L,
  width = 20L,
  height = 20L,
  n_sheep = 100L,
  n_wolves = 25L
)
}
\arguments{
\item{params}{Named list — typically LV parameters (\code{alpha}, \code{beta},
\code{gamma}, \code{delta}) identifying the ecological regime being studied.
Currently stored for reference only; the ABM uses independent default
energy/reproduction parameters.}

\item{n_runs}{Number of independent replications. Default \code{20L}.}

\item{steps}{Number of ticks per run. Default \code{500L}.}

\item{width}{Grid width. Default \code{20L}.}

\item{height}{Grid height. Default \code{20L}.}

\item{n_sheep}{Initial sheep count. Default \code{100L}.}

\item{n_wolves}{Initial wolf count. Default \code{25L}.}
}
\value{
Named list:
\itemize{
\item \code{extinction_prob}: fraction of runs where either population hit 0.
\item \code{mean_time_to_extinction}: mean step at first extinction event across
extinct runs; \code{NA} if no run went extinct.
\item \code{runs}: tibble with columns \code{run}, \code{extinct}, \code{ext_step} (step of
first extinction event, or \code{NA} if not extinct).
}
}
\description{
Runs the predator-prey ABM with \code{n_runs} independent seeds and computes
the fraction of runs in which either population reached zero. Designed
for Activity 1 (ODE vs ABM extinction comparison). The \code{params} argument
accepts a named list of Lotka-Volterra parameters for documentation
purposes; the ABM uses its own default energy/reproduction parameters,
which are ecologically comparable to the LV defaults.
}
\examples{
ep <- find_abm_extinction_probability(params = LV_DEFAULTS, n_runs = 5L,
                                      steps = 200L)
ep$extinction_prob
}
