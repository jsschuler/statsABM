% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_activities.R
\name{plot_abm_ode_comparison}
\alias{plot_abm_ode_comparison}
\title{Plot multiple ABM density runs against an ODE trajectory}
\usage{
plot_abm_ode_comparison(
  abm_list,
  ode,
  n_sheep = 100L,
  n_prey = 40,
  theme_fn = theme_course_light
)
}
\arguments{
\item{abm_list}{Named list of ABM run results (each from
\code{PredatorPreyModel$run()}). Names become panel sub-titles (e.g.
\code{list(sparse = ..., medium = ..., dense = ...)}).}

\item{ode}{Result list from \code{run_lv()}.}

\item{n_sheep}{Initial sheep count used in the ABM runs. Used to scale
the ODE trajectory onto the same y-axis. Default \code{100L}.}

\item{n_prey}{Initial prey count used by the ODE. Default \code{40} (matches
\code{LV_DEFAULTS} initial condition \code{y0 = c(prey = 40, predators = 9)}).}

\item{theme_fn}{A ggplot2 theme function. Default \code{theme_course_light}.}
}
\value{
A patchwork object.
}
\description{
Shows a row of time-series panels — one for each entry in \code{abm_list} —
with the ODE trajectory scaled to the ABM's initial population displayed
in each panel for direct comparison. Used in Activity 2 to make the
density-dependence of the mean-field approximation visually explicit.
}
\examples{
ode   <- run_lv()
dense <- PredatorPreyModel$new(width=20L, height=20L, seed=42L)$run(300L)
plot_abm_ode_comparison(list(dense = dense), ode)
}
