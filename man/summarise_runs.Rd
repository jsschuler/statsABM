% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_activities.R
\name{summarise_runs}
\alias{summarise_runs}
\title{Summarise key statistics across a list of pandemic model runs}
\usage{
summarise_runs(runs)
}
\arguments{
\item{runs}{List of result lists from \code{PandemicModel$run()}.}
}
\value{
Tibble with columns \code{run} (integer or \code{"mean"} / \code{"sd"}),
\code{peak_I}, \code{peak_step}, \code{final_R}, \code{R0_empirical}.
}
\description{
Extracts \code{peak_I}, \code{peak_step}, \code{final_R}, and \code{R0_empirical} from each
run and returns them in a tidy tibble, with a trailing summary row
containing the mean and SD across runs.
}
\examples{
runs <- lapply(1:3, function(s) PandemicModel$new(seed=s)$run(50L))
summarise_runs(runs)
}
