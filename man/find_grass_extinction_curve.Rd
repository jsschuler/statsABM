% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_activities.R
\name{find_grass_extinction_curve}
\alias{find_grass_extinction_curve}
\title{Compute extinction probability across grass regrowth times}
\usage{
find_grass_extinction_curve(
  regrowth_times,
  n_runs = 20L,
  steps = 500L,
  width = 20L,
  height = 20L
)
}
\arguments{
\item{regrowth_times}{Integer vector of \code{grass_regrowth_time} values.}

\item{n_runs}{Number of replications per regrowth time. Default \code{20L}.}

\item{steps}{Steps per run. Default \code{500L}.}

\item{width}{Grid width. Default \code{20L}.}

\item{height}{Grid height. Default \code{20L}.}
}
\value{
Tibble with columns \code{regrowth_time} (integer), \code{extinction_prob}
(numeric in \link{0, 1}), \code{se} (standard error of proportion).
}
\description{
For each value in \code{regrowth_times}, runs \code{n_runs} independent
\code{PredatorPreyGrassModel} replicates and computes the fraction that went
extinct within \code{steps} ticks. Returns a tibble suitable for
\code{plot_extinction_curve()}.
}
\examples{
curve <- find_grass_extinction_curve(c(10L, 30L, 60L), n_runs = 3L,
                                     steps = 100L)
curve
}
